idp-service:
  nameOverride: logging-service
  image:
    repository: idp-platform/logging-service
    tag: local
  service:
    port: 80
    targetPort: 3007
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  hpa:
    minReplicas: 1
    maxReplicas: 3
  env:
    NODE_ENV: production
    PORT: "3007"
    JWT_ISSUER: idp-platform
  secretData:
    JWT_SECRET: local-kind-cluster-secret-change-for-real-deployments
