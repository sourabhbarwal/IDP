name: dora-service CI

on:
  push:
    branches: [main, develop]
    paths:
      - "backend/dora-service/**"
      - "backend/common/**"
      - "backend/package.json"
      - "backend/package-lock.json"
      - "backend/tsconfig.base.json"
      - ".github/workflows/dora-service-ci.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "backend/dora-service/**"
      - "backend/common/**"
      - "backend/package.json"
      - "backend/package-lock.json"
      - "backend/tsconfig.base.json"
      - ".github/workflows/dora-service-ci.yml"

env:
  NODE_VERSION: "20"

jobs:
  lint-and-unit-test:
    name: Lint & Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: backend/package-lock.json
      - working-directory: backend
        run: npm ci
      - working-directory: backend
        run: npm run build --workspace=common
      - working-directory: backend/dora-service
        run: npm run lint
      - working-directory: backend/dora-service
        run: npm run test:cov
        env: { CI: true }
      - name: Audit production dependencies
        working-directory: backend
        run: npm audit --audit-level=critical --omit=dev --workspace=dora-service
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dora-svc-coverage
          path: backend/dora-service/coverage/

  build:
    name: TypeScript Build
    runs-on: ubuntu-latest
    needs: lint-and-unit-test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: backend/package-lock.json
      - working-directory: backend
        run: npm ci
      - working-directory: backend
        run: npm run build --workspace=common
      - working-directory: backend
        run: npm run build --workspace=dora-service

  docker-build-and-scan:
    name: Docker Build & Scan
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
      security-events: write
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/dora-service/Dockerfile
          push: false
          load: true
          tags: dora-service:${{ github.sha }}
          build-args: |
            BUILD_REVISION=${{ github.sha }}
            BUILD_DATE=${{ github.event.repository.updated_at }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Scan with Trivy
        uses: aquasecurity/trivy-action@v0.24.0
        with:
          image-ref: dora-service:${{ github.sha }}
          format: sarif
          output: trivy-results.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'
          ignore-unfixed: true
      
      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image to GHCR
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          docker tag dora-service:${{ github.sha }} \
            ghcr.io/${{ github.repository_owner }}/idp-dora-service:sha-${SHORT_SHA}
          docker tag dora-service:${{ github.sha }} \
            ghcr.io/${{ github.repository_owner }}/idp-dora-service:latest
          docker push ghcr.io/${{ github.repository_owner }}/idp-dora-service:sha-${SHORT_SHA}
          docker push ghcr.io/${{ github.repository_owner }}/idp-dora-service:latest
          echo "✅ Pushed idp-dora-service:sha-${SHORT_SHA} to GHCR"

    